databases:
  - name: todolist-db
    databaseName: todolistdb
    user: todolistuser
    plan: free

services:
  - type: web
    name: todolist-api-production
    env: docker
    plan: free
    envVars:
      - key: ConnectionStrings__DefaultConnection
        fromDatabase:
          name: todolist-db
          property: connectionString
